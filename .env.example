# .env.example - этот файл КОММИТИТСЯ в Git
# После клонирования: cp .env.example .env

# Server Configuration
PORT=8443
NODE_ENV=production

# SSL Certificates (укажите пути к вашим сертификатам)
SSL_KEY_PATH=./privkey.pem
SSL_CERT_PATH=./fullchain.pem

# Database
DB_PATH=./data/database.sqlite

# Для production замените на реальные значения в .env

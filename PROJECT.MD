# PROJECT.md — Документация проекта pelikan-admin-docker

## О проекте
Проект **pelikan-admin-docker** — это админ-панель с серверной частью на Node.js, предназначенная для управления меню и расписанием сайта или приложения. Проект упакован в Docker-контейнер с использованием SQLite для хранения данных и HTTPS для обеспечения безопасности.


## Структура проекта
```
pelikan-admin-docker/
├── data/                      # Хранилище данных SQLite и текстовых файлов
│   ├── menu.txt               # Текстовое меню, редактируемое пользователем
│   ├── schedule.txt           # Текстовое расписание
│   └── database.sqlite        # SQLite база данных
├── public/                    # Фронтенд админ-панели (HTML/JS/CSS)
│   ├── index.html             # Основной интерфейс с textarea для редактирования
│   ├── app.js                 # Логика конвертации и отправки данных
│   └── styles.css             # Стили для админ-панели
├── Dockerfile                 # Описание образа Docker
├── docker-compose.yml         # Конфигурация Docker Compose
├── server.js                  # Сервер Node.js с API и логикой работы с файлами
├── package.json               # Node.js зависимости и скрипты
├── privkey.pem                # SSL ключ для HTTPS (НЕ коммитить в репо)
├── fullchain.pem              # SSL сертификат для HTTPS (НЕ коммитить в репо)
├── README.md                  # Инструкция по использованию
└── .gitignore                 # Исключить секюрные и временные файлы
```

## Установка и запуск

1. Клонировать репозиторий и перейти в папку проекта
2. Создать папку `data/` и добавить туда файлы `menu.txt` и `schedule.txt` (шаблоны имеются в репозитории)
3. Скопировать SSL-сертификаты `privkey.pem` и `fullchain.pem` в корень (НЕ выкладывать в репозиторий)
4. Собрать и запустить контейнеры Docker:
```
docker-compose build
docker-compose up -d
```
5. Открыть админ-панель по адресу: https://localhost:8443 или https://<IP_сервера>:8443

## API

- GET `/api/getMenuText` — получить исходный текст меню
- POST `/api/saveMenuText` — сохранить сторонний текст меню; автоматически конвертируется в JSON

## Безопасность

- Для HTTPS используется SSL сертификаты
- В продакшене рекомендуется настроить аутентификацию и firewall
- Никогда не храните секреты и сертификаты в публичных репозиториях

## CI/CD

- Настройте GitHub Actions workflow для автоматической сборки Docker-образа и деплоя на VPS
- Храните секреты GitHub в GitHub Secrets (DOCKER_USERNAME, VPS_SSH_KEY и др.)

## Контакты

По вопросам и развитию проекта обращайтесь к разработчику.

***

Этот файл должен быть размещён в корне репозитория pelikan-admin-docker как основная документация проекта.

Если нужно, могу помочь также составить README.md и другие вспомогательные файлы.

[1](https://habr.com/ru/articles/725132/)
[2](https://sites.google.com/site/ndmisharin/home/tutorials/readme)
[3](https://doka.guide/recipes/github-add-readme/)
[4](https://www.youtube.com/watch?v=VQAX_W2cXQc)
[5](https://htmlacademy.ru/blog/git/markdown)
[6](https://wiki.merionet.ru/articles/kak-napisat-xorosii-readme-fail-dlia-vasego-proekta-github)
[7](https://lyucean.com/how-to-write-readme/)
[8](https://ru.hexlet.io/blog/posts/kak-napisat-horoshiy-readme-dlya-vashego-proekta-na-github)
[9](https://gist.github.com/Jekins/2bf2d0638163f1294637)


# PROJECT.md — Документация проекта pelikan-admin-docker

## О проекте
Проект **pelikan-admin-docker** — это админ-панель с серверной частью на Node.js, предназначенная для управления меню и расписанием сайта или приложения. Проект упакован в Docker-контейнер с использованием SQLite для хранения данных и HTTPS для обеспечения безопасности.

## Структура проекта
```
pelikan-admin-docker/
├── data/                      # Хранилище данных SQLite и текстовых файлов
│   ├── menu.txt               # Текстовое меню, редактируемое пользователем
│   ├── schedule.txt           # Текстовое расписание
│   └── database.sqlite        # SQLite база данных
├── public/                    # Фронтенд админ-панели (HTML/JS/CSS)
│   ├── index.html             # Основной интерфейс с textarea для редактирования
│   ├── app.js                 # Логика конвертации и отправки данных
│   └── styles.css             # Стили для админ-панели
├── Dockerfile                 # Описание образа Docker
├── docker-compose.yml         # Конфигурация Docker Compose
├── server.js                  # Сервер Node.js с API и логикой работы с файлами
├── package.json               # Node.js зависимости и скрипты
├── privkey.pem                # SSL ключ для HTTPS (НЕ коммитить в репо)
├── fullchain.pem              # SSL сертификат для HTTPS (НЕ коммитить в репо)
├── README.md                  # Инструкция по использованию
└── .gitignore                 # Исключить секюрные и временные файлы
```

## Установка и запуск

1. Клонировать репозиторий и перейти в папку проекта
2. Создать папку `data/` и добавить туда файлы `menu.txt` и `schedule.txt` (шаблоны имеются в репозитории)
3. Скопировать SSL-сертификаты `privkey.pem` и `fullchain.pem` в корень (НЕ выкладывать в репозиторий)
4. Собрать и запустить контейнеры Docker:
```
docker-compose build
docker-compose up -d
```
5. Открыть админ-панель по адресу: https://localhost:8443 или https://<IP_сервера>:8443

## API

- GET `/api/getMenuText` — получить исходный текст меню
- POST `/api/saveMenuText` — сохранить сторонний текст меню; автоматически конвертируется в JSON

## Безопасность

- Для HTTPS используется SSL сертификаты
- В продакшене рекомендуется настроить аутентификацию и firewall
- Никогда не храните секреты и сертификаты в публичных репозиториях

## CI/CD

- Настройте GitHub Actions workflow для автоматической сборки Docker-образа и деплоя на VPS
- Храните секреты GitHub в GitHub Secrets (DOCKER_USERNAME, VPS_SSH_KEY и др.)

## Контакты

По вопросам и развитию проекта обращайтесь к разработчику.

***

Этот файл должен быть размещён в корне репозитория pelikan-admin-docker как основная документация проекта.

Если нужно, могу помочь также составить README.md и другие вспомогательные файлы.

[1](https://habr.com/ru/articles/725132/)
[2](https://sites.google.com/site/ndmisharin/home/tutorials/readme)
[3](https://doka.guide/recipes/github-add-readme/)
[4](https://www.youtube.com/watch?v=VQAX_W2cXQc)
[5](https://htmlacademy.ru/blog/git/markdown)
[6](https://wiki.merionet.ru/articles/kak-napisat-xorosii-readme-fail-dlia-vasego-proekta-github)
[7](https://lyucean.com/how-to-write-readme/)
[8](https://ru.hexlet.io/blog/posts/kak-napisat-horoshiy-readme-dlya-vashego-proekta-na-github)
[9](https://gist.github.com/Jekins/2bf2d0638163f1294637)# PROJECT.md — Документация проекта pelikan-admin-docker

## О проекте
Проект **pelikan-admin-docker** — это админ-панель с серверной частью на Node.js, предназначенная для управления меню и расписанием сайта или приложения. Проект упакован в Docker-контейнер с использованием SQLite для хранения данных и HTTPS для обеспечения безопасности.

## Структура проекта
```
pelikan-admin-docker/
├── data/                      # Хранилище данных SQLite и текстовых файлов
│   ├── menu.txt               # Текстовое меню, редактируемое пользователем
│   ├── schedule.txt           # Текстовое расписание
│   └── database.sqlite        # SQLite база данных
├── public/                    # Фронтенд админ-панели (HTML/JS/CSS)
│   ├── index.html             # Основной интерфейс с textarea для редактирования
│   ├── app.js                 # Логика конвертации и отправки данных
│   └── styles.css             # Стили для админ-панели
├── Dockerfile                 # Описание образа Docker
├── docker-compose.yml         # Конфигурация Docker Compose
├── server.js                  # Сервер Node.js с API и логикой работы с файлами
├── package.json               # Node.js зависимости и скрипты
├── privkey.pem                # SSL ключ для HTTPS (НЕ коммитить в репо)
├── fullchain.pem              # SSL сертификат для HTTPS (НЕ коммитить в репо)
├── README.md                  # Инструкция по использованию
└── .gitignore                 # Исключить секюрные и временные файлы
```

## Установка и запуск

1. Клонировать репозиторий и перейти в папку проекта
2. Создать папку `data/` и добавить туда файлы `menu.txt` и `schedule.txt` (шаблоны имеются в репозитории)
3. Скопировать SSL-сертификаты `privkey.pem` и `fullchain.pem` в корень (НЕ выкладывать в репозиторий)
4. Собрать и запустить контейнеры Docker:
```
docker-compose build
docker-compose up -d
```
5. Открыть админ-панель по адресу: https://localhost:8443 или https://<IP_сервера>:8443

## API

- GET `/api/getMenuText` — получить исходный текст меню
- POST `/api/saveMenuText` — сохранить сторонний текст меню; автоматически конвертируется в JSON

## Безопасность

- Для HTTPS используется SSL сертификаты
- В продакшене рекомендуется настроить аутентификацию и firewall
- Никогда не храните секреты и сертификаты в публичных репозиториях

## CI/CD

- Настройте GitHub Actions workflow для автоматической сборки Docker-образа и деплоя на VPS
- Храните секреты GitHub в GitHub Secrets (DOCKER_USERNAME, VPS_SSH_KEY и др.)

## Контакты

По вопросам и развитию проекта обращайтесь к разработчику.

***

Этот файл должен быть размещён в корне репозитория pelikan-admin-docker как основная документация проекта.

Если нужно, могу помочь также составить README.md и другие вспомогательные файлы.

[1](https://habr.com/ru/articles/725132/)
[2](https://sites.google.com/site/ndmisharin/home/tutorials/readme)
[3](https://doka.guide/recipes/github-add-readme/)
[4](https://www.youtube.com/watch?v=VQAX_W2cXQc)
[5](https://htmlacademy.ru/blog/git/markdown)
[6](https://wiki.merionet.ru/articles/kak-napisat-xorosii-readme-fail-dlia-vasego-proekta-github)
[7](https://lyucean.com/how-to-write-readme/)
[8](https://ru.hexlet.io/blog/posts/kak-napisat-horoshiy-readme-dlya-vashego-proekta-na-github)
[9](https://gist.github.com/Jekins/2bf2d0638163f1294637)# PROJECT.md — Документация проекта pelikan-admin-docker

## О проекте
Проект **pelikan-admin-docker** — это админ-панель с серверной частью на Node.js, предназначенная для управления меню и расписанием сайта или приложения. Проект упакован в Docker-контейнер с использованием SQLite для хранения данных и HTTPS для обеспечения безопасности.

## Структура проекта
```
pelikan-admin-docker/
├── data/                      # Хранилище данных SQLite и текстовых файлов
│   ├── menu.txt               # Текстовое меню, редактируемое пользователем
│   ├── schedule.txt           # Текстовое расписание
│   └── database.sqlite        # SQLite база данных
├── public/                    # Фронтенд админ-панели (HTML/JS/CSS)
│   ├── index.html             # Основной интерфейс с textarea для редактирования
│   ├── app.js                 # Логика конвертации и отправки данных
│   └── styles.css             # Стили для админ-панели
├── Dockerfile                 # Описание образа Docker
├── docker-compose.yml         # Конфигурация Docker Compose
├── server.js                  # Сервер Node.js с API и логикой работы с файлами
├── package.json               # Node.js зависимости и скрипты
├── privkey.pem                # SSL ключ для HTTPS (НЕ коммитить в репо)
├── fullchain.pem              # SSL сертификат для HTTPS (НЕ коммитить в репо)
├── README.md                  # Инструкция по использованию
└── .gitignore                 # Исключить секюрные и временные файлы
```

## Установка и запуск

1. Клонировать репозиторий и перейти в папку проекта
2. Создать папку `data/` и добавить туда файлы `menu.txt` и `schedule.txt` (шаблоны имеются в репозитории)
3. Скопировать SSL-сертификаты `privkey.pem` и `fullchain.pem` в корень (НЕ выкладывать в репозиторий)
4. Собрать и запустить контейнеры Docker:
```
docker-compose build
docker-compose up -d
```
5. Открыть админ-панель по адресу: https://localhost:8443 или https://<IP_сервера>:8443

## API

- GET `/api/getMenuText` — получить исходный текст меню
- POST `/api/saveMenuText` — сохранить сторонний текст меню; автоматически конвертируется в JSON

## Безопасность

- Для HTTPS используется SSL сертификаты
- В продакшене рекомендуется настроить аутентификацию и firewall
- Никогда не храните секреты и сертификаты в публичных репозиториях

## CI/CD

- Настройте GitHub Actions workflow для автоматической сборки Docker-образа и деплоя на VPS
- Храните секреты GitHub в GitHub Secrets (DOCKER_USERNAME, VPS_SSH_KEY и др.)

## Контакты

По вопросам и развитию проекта обращайтесь к разработчику.

***

Этот файл должен быть размещён в корне репозитория pelikan-admin-docker как основная документация проекта.
pelikan-admin-docker/
├── data/                      # Хранилище данных SQLite и текстовых файлов
│   ├── menu.txt               # Текстовое меню, редактируемое пользователем
│   ├── schedule.txt           # Текстовое расписание
│   └── database.sqlite        # SQLite база данных
├── public/                    # Фронтенд админ-панели (HTML/JS/CSS)
│   ├── index.html             # Основной интерфейс с textarea для редактирования
│   ├── app.js                 # Логика конвертации и отправки данных
│   └── styles.css             # Стили для админ-панели
├── Dockerfile                 # Описание образа Docker
├── docker-compose.yml         # Конфигурация Docker Compose
├── server.js                  # Сервер Node.js с API и логикой работы с файлами
├── package.json               # Node.js зависимости и скрипты
├── privkey.pem                # SSL ключ для HTTPS (НЕ коммитить в репо)
├── fullchain.pem              # SSL сертификат для HTTPS (НЕ коммитить в репо)
├── README.md                  # Инструкция по использованию
└── .gitignore                 # Исключить секюрные и временные файлы
PROJECT.md — Документация проекта pelikan-admin-docker
О проекте
Проект pelikan-admin-docker — это админ-панель с серверной частью на Node.js, предназначенная для управления меню и расписанием сайта или приложения. Проект упакован в Docker-контейнер с использованием SQLite для хранения данных и HTTPS для обеспечения безопасности.

Структура проекта


Установка и запуск
Клонировать репозиторий и перейти в папку проекта

Создать папку data/ и добавить туда файлы menu.txt и schedule.txt (шаблоны имеются в репозитории)

Скопировать SSL-сертификаты privkey.pem и fullchain.pem в корень (НЕ выкладывать в репозиторий)

Собрать и запустить контейнеры Docker:

docker-compose build
docker-compose up -d

Открыть админ-панель по адресу: https://localhost:8443 или https://<IP_сервера>:8443

API
GET /api/getMenuText — получить исходный текст меню

POST /api/saveMenuText — сохранить сторонний текст меню; автоматически конвертируется в JSON

Безопасность
Для HTTPS используется SSL сертификаты

В продакшене рекомендуется настроить аутентификацию и firewall

Никогда не храните секреты и сертификаты в публичных репозиториях

CI/CD
Настройте GitHub Actions workflow для автоматической сборки Docker-образа и деплоя на VPS

Храните секреты GitHub в GitHub Secrets (DOCKER_USERNAME, VPS_SSH_KEY и др.)

Контакты
По вопросам и развитию проекта обращайтесь к разработчику.

Этот файл должен быть размещён в корне репозитория pelikan-admin-docker как основная документация проекта.

Если нужно, могу помочь также составить README.md и другие вспомогательные файлы.



09.11.2025__20:42 # PROJECT.md для pelikan-admin-docker﻿

## О проекте﻿

Проект pelikan-admin-docker — это админ-панель с backend на Node.js, предназначенная для управления меню и расписанием сайта или мобильного приложения. Проект упакован в Docker с использованием SQLite и HTTPS.

## Структура проекта﻿

text
pelikan-admin-docker/
├── data/                     # Хранилище файлов меню, расписания, база данных
├── public/                   # Фронтенд админ-панели (HTML/JS/CSS)
├── Dockerfile                # Инструкция сборки Docker-образа
├── docker-compose.yml        # Конфигурация запуска контейнеров
├── server.js                 # Сервер Node.js с API
├── package.json              # Зависимости проекта
├── README.md                 # Описание проекта
├── privkey.pem               # SSL ключ (НЕ коммитить)
├── fullchain.pem             # SSL сертификат (НЕ коммитить)
└── .gitignore                # Исключённые файлы
## Установка и запуск﻿

Клонируйте репозиторий и перейдите в папку проекта:

text
git clone https://github.com/Andreyhiitola/pelikan-admin-docker.git
cd pelikan-admin-docker
Создайте папку data/ и добавьте туда файлы menu.txt, schedule.txt (шаблоны есть в репо).

Скопируйте свои SSL сертификаты privkey.pem, fullchain.pem в корень проекта (без коммита).

Соберите и запустите Docker-контейнеры:

text
docker-compose build
docker-compose up -d
Откройте браузер по адресу https://localhost:8443 или https://<IP_сервера>:8443.

## API﻿

GET /api/getMenuText — получить исходный текст меню

POST /api/saveMenuText — отправить текст меню, который будет сохранён и конвертирован в JSON

## Технологии﻿

Node.js с Express

SQLite для хранения данных

Docker и Docker Compose

HTTPS с SSL

## Безопасность﻿

Используйте ваши SSL сертификаты для HTTPS

Настройте firewall и ограничьте доступ к админ-панели

Не храните ключи и сертификаты в публичных репозиториях

## CI/CD﻿

Используйте GitHub Actions для автоматической сборки образа и деплоя на VPS

Держите секреты в GitHub Secrets (Docker Hub креды, SSH ключи и др.)

## Контакты﻿
VPS_SSH_KEY — приватный SSH ключ для доступа к VPS/ я так понимаю надо вз
По всем вопросам и предложениям обращайтесь к разработчику.VPS_SSH_KEY — приватный SSH ключ для доступа к VPS/ я так понимаю надо вз

08.11.2025_21:00  # PROJECT.md для pelikan-admin-docker﻿

## О проекте﻿
Проект pelikan-admin-docker — это админ-панель с backend на Node.js, упакованная в Docker для управления меню и расписанием, с поддержкой HTTPS через SSL.
pelikan-admin-docker/
├── data/                   # Данные (БД, текст меню и расписания)
├── docker-compose.yml      # Для оркестрации Docker-контейнеров
├── privkey.pem             # SSL приватный ключ
├── fullchain.pem           # SSL сертификат
└── (опционально) скрипты для деплоя и обновления сертификатов
## Использование﻿

Админ-панель доступна через браузер по HTTPS, работает с backend API на VPS.

Backend API обеспечивает синхронизацию данных с сайтом и Android-приложением.

Docker-образы собираются из Dockerfile в репозитории GitHub.

На VPS разворачивается docker-compose для запуска контейнеров.

SSL сертификаты генерируются через Certbot, можно использовать Duck DNS для динамического IP.

## Настройка SSL и Duck DNS﻿

Используйте Certbot с поддержкой Duck DNS для бесплатного получения и автоматического обновления сертификатов.

Путь к SSL файлам указывается в docker-compose и монтируется в контейнеры.

## CI/CD и автоматизация﻿

GitHub Actions собирает образ из Dockerfile и деплоит на VPS по SSH.

Используйте секреты для Docker Hub и SSH-доступа.

Автоматический скрипт обновляет сертификаты и перезагружает контейнеры.

## Важное﻿

Dockerfile хранится в GitHub, на VPS — только docker-compose, данные и SSL.

Для работы с API сайт и мобильное приложение обращаются к backend на VPS.

При отсутствии домена можно использовать Duck DNS для тестирования.

## Контакты﻿
Для помощи с настройкой обращайтесь к разработчику.

Если хотите, могу помочь с примером скрипта и инструкциями по настройке Certbot на 
sudo apt update
sudo apt install -y certbot
sudo certbot certonly --standalone -d yourdomain.com -d www.yourdomain.com
#!/bin/bash

# Пути сертификатов
CERT_PATH="/etc/letsencrypt/live/yourdomain.com"
PROJECT_SSL_PATH="/path/to/pelikan-admin-docker"

# Копируем сертификаты в проект
cp "$CERT_PATH/privkey.pem" "$PROJECT_SSL_PATH/privkey.pem"
cp "$CERT_PATH/fullchain.pem" "$PROJECT_SSL_PATH/fullchain.pem"

# Перезапускаем docker-compose (замените на свой путь)
cd "$PROJECT_SSL_PATH"
docker-compose down
docker-compose up -d
0 0,12 * * * /usr/bin/certbot renew --quiet --post-hook "/path/to/restart-script.sh"
Где /path/to/restart-script.sh — это путь к вашему скрипту из шага 3, который копирует сертификаты и перезапускает контейнеры.

PROJECT.MD jn 10/11/2025
# Pelikan Admin Docker - Проект с автоматическим CI/CD

Веб-приложение для управления данными с автоматическим деплоем на VPS через GitHub Actions.

## 📋 Содержание

- [Описание](#описание)
- [Архитектура](#архитектура)
- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [CI/CD Pipeline](#cicd-pipeline)
- [Конфигурация](#конфигурация)
- [API Endpoints](#api-endpoints)
- [Deployment](#deployment)
- [Мониторинг и логи](#мониторинг-и-логи)
- [Troubleshooting](#troubleshooting)

---

## 🎯 Описание

Pelikan Admin — это веб-приложение с админ-панелью для управления данными, развернутое в Docker контейнерах с автоматическим CI/CD через GitHub Actions.

### Основные возможности

- 📝 Управление данными через веб-интерфейс
- 🔐 HTTPS подключение с SSL сертификатами
- 🚀 Автоматический деплой при push в main
- 🐳 Контейнеризация с Docker
- 📊 Хранение данных в SQLite
- 🔄 Автоматическое обновление при изменениях в коде

---

## 🏗 Архитектура
┌─────────────────┐
│ GitHub Repo │
│ (main branch) │
└────────┬────────┘
│ push
▼
┌─────────────────┐
│ GitHub Actions │
│ - Build image │
│ - Push to Hub │
│ - Deploy │
└────────┬────────┘
│ SSH
▼
┌─────────────────┐
│ VPS │
│ ┌───────────┐ │
│ │ Docker │ │
│ │ Container │ │
│ │ Node.js │ │
│ │ :8443 │ │
│ └───────────┘ │
└─────────────────┘


---

## 🛠 Технологии

### Backend
- **Node.js** — серверная платформа
- **Express.js** — веб-фреймворк
- **SQLite** — база данных
- **HTTPS** — безопасное соединение

### DevOps
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация
- **GitHub Actions** — CI/CD
- **SSH** — удаленный деплой

### Инфраструктура
- **VPS** — хостинг приложения
- **Docker Hub** — реестр образов
- **Self-signed SSL** — сертификаты для тестирования

---

## 📁 Структура проекта
pelikan-admin-docker/
├── .github/
│ └── workflows/
│ └── ci-cd.yml # GitHub Actions workflow
├── public/ # Статические файлы
│ ├── index.html # Админ-панель
│ ├── app.js # Frontend логика
│ └── styles.css # Стили
├── data/ # База данных (создается автоматически)
│ └── database.sqlite # SQLite БД
├── server.js # Backend сервер
├── package.json # Node.js зависимости
├── Dockerfile # Docker образ
├── docker-compose.yml # Docker Compose конфигурация
├── .env.example # Пример переменных окружения
├── privkey.pem # SSL приватный ключ (генерируется)
├── fullchain.pem # SSL сертификат (генерируется)
├── README.md # Основная документация
└── PROJECT.md # Этот файл

---

## 🚀 Установка и запуск

### Локальная разработка

Клонировать репозиторий
git clone https://github.com/Andreyhiitola/pelikan-admin-docker.git
cd pelikan-admin-docker

Установить зависимости
npm install

Создать SSL сертификаты для локального тестирования
openssl req -x509 -newkey rsa:2048 -nodes
-keyout privkey.pem
-out fullchain.pem
-days 365
-subj "/CN=localhost"

Запустить сервер
node server.js

Открыть в браузере
https://localhost:8443

### Запуск в Docker (локально)
Собрать образ
docker build -t pelikan-admin .

Запустить контейнер
docker run -d
-p 8443:8443
-v $(pwd)/data:/app/data
-v $(pwd)/privkey.pem:/app/privkey.pem:ro
-v $(pwd)/fullchain.pem:/app/fullchain.pem:ro
--name pelikan-admin
pelikan-admin

Проверить логи
docker logs -f pelikan-admin


### Запуск с Docker Compose

Запустить
docker-compose up -d

Проверить статус
docker-compose ps

Посмотреть логи
docker-compose logs -f

Остановить
docker-compose down

---

## 🔄 CI/CD Pipeline

### Workflow структура
name: Build and Deploy to VPS

on:
push:
branches: [main]

jobs:
build-and-push:
# Сборка Docker образа и push в Docker Hub

deploy:
# SSH подключение к VPS и деплой

### Этапы деплоя

1. **Trigger:** Push в ветку `main`
2. **Build:** Сборка Docker образа
3. **Push:** Загрузка образа в Docker Hub
4. **Deploy:** 
   - SSH подключение к VPS
   - Обновление кода из Git
   - Генерация SSL (если нужно)
   - Pull нового образа
   - Перезапуск контейнеров
   - Проверка статуса

### GitHub Secrets

Необходимые секреты в `Settings → Secrets and variables → Actions`:
DOCKER_PASSWORD # Пароль Docker Hub
VPS_HOST # IP адрес VPS
VPS_USER # SSH пользователь (обычно root)
VPS_SSH_KEY # Приватный SSH ключ
DOCKERHUB_USERNAME # Логин Docker Hub

---

## ⚙️ Конфигурация

### docker-compose.yml
version: '3.8'

services:
app:
image: andreysagurov/pelikan-admin:latest
container_name: pelikan-admin
restart: unless-stopped
ports:
- "8443:8443"
volumes:
- ./data:/app/data
- ./privkey.pem:/app/privkey.pem:ro
- ./fullchain.pem:/app/fullchain.pem:ro
environment:
- NODE_ENV=production
- PORT=8443
networks:
- pelikan-network

networks:
pelikan-network:
driver: bridge


### Переменные окружения

Создайте `.env` файл на основе `.env.example`:
NODE_ENV=production
PORT=8443
SSL_KEY_PATH=/app/privkey.pem
SSL_CERT_PATH=/app/fullchain.pem
DATABASE_PATH=/app/data/database.sqlite


---

## 🌐 API Endpoints

### GET /
Возвращает админ-панель (index.html)

### GET /api/data
Получить все записи из базы данных

**Response:**

[
{
"id": 1,
"name": "Example",
"value": "Data",
"created_at": "2025-11-10T12:00:00Z"
}
]


### POST /api/data
Добавить новую запись

**Request:**
{
"name": "New Item",
"value": "Some data"
}

### PUT /api/data/:id
Обновить запись

### DELETE /api/data/:id
Удалить запись

---

## 🚢 Deployment

### Первичная настройка VPS


### PUT /api/data/:id
Обновить запись

### DELETE /api/data/:id
Удалить запись

---

## 🚢 Deployment

### Первичная настройка VPS
1. Установить Docker
curl -fsSL https://get.docker.com | sh
systemctl start docker
systemctl enable docker

2. Установить Docker Compose
apt install docker-compose -y

3. Создать директорию проекта
mkdir -p /opt/pelikan-admin-docker
cd /opt/pelikan-admin-docker

4. Клонировать репозиторий
git clone https://github.com/Andreyhiitola/pelikan-admin-docker.git .

5. Создать SSL сертификаты
openssl req -x509 -newkey rsa:2048 -nodes
-keyout privkey.pem
-out fullchain.pem
-days 365
-subj "/CN=localhost"

6. Открыть порт в firewall
ufw allow 8443/tcp

7. Запустить приложение
docker-compose up -d

text

### Настройка SSH для GitHub Actions

На локальной машине
ssh-keygen -t ed25519 -f ~/.ssh/pelikan_deploy -N ""

Скопировать публичный ключ на VPS
ssh-copy-id -i ~/.ssh/pelikan_deploy.pub root@vps-ip

Скопировать приватный ключ в GitHub Secret VPS_SSH_KEY
cat ~/.ssh/pelikan_deploy

text

### Ручной деплой

На VPS
cd /opt/pelikan-admin-docker

Обновить код
git pull origin main

Пересобрать и перезапустить
docker-compose down
docker-compose up -d --build

Или просто pull нового образа
docker pull andreysagurov/pelikan-admin:latest
docker-compose up -d --force-recreate

text

---

## 📊 Мониторинг и логи

### Просмотр логов

Логи контейнера
docker-compose logs -f

Последние 100 строк
docker-compose logs --tail=100

Логи конкретного сервиса
docker-compose logs -f app

Логи Docker
journalctl -u docker -f

text

### Проверка статуса

Статус контейнеров
docker-compose ps

Детальная информация
docker inspect pelikan-admin

Проверка порта
ss -tulpn | grep 8443
netstat -tulpn | grep 8443

Использование ресурсов
docker stats pelikan-admin

text

### Мониторинг деплоя

Откройте GitHub Actions:
https://github.com/Andreyhiitola/pelikan-admin-docker/actions

text

Каждый деплой создает workflow run с полными логами.

---

## 🔧 Troubleshooting

### Проблема: Контейнер не запускается

Проверить логи
docker-compose logs

Проверить, что образ существует
docker images | grep pelikan-admin

Пересоздать контейнер
docker-compose down
docker-compose up -d --force-recreate

text

### Проблема: Порт 8443 не отвечает

Проверить, что контейнер работает
docker-compose ps

Проверить порт внутри контейнера
docker exec pelikan-admin netstat -tulpn | grep 8443

Проверить firewall
ufw status
ufw allow 8443/tcp

Проверить, не занят ли порт
lsof -i :8443

text

### Проблема: SSL сертификат

Пересоздать сертификаты
cd /opt/pelikan-admin-docker
rm -f privkey.pem fullchain.pem

openssl req -x509 -newkey rsa:2048 -nodes
-keyout privkey.pem
-out fullchain.pem
-days 365
-subj "/CN=localhost"

chmod 600 privkey.pem
chmod 644 fullchain.pem

Перезапустить
docker-compose restart

text

### Проблема: GitHub Actions деплой падает

Проверить секреты
Settings → Secrets → Actions
Должны быть: VPS_HOST, VPS_USER, VPS_SSH_KEY, DOCKER_PASSWORD
Проверить SSH подключение вручную
ssh -i ~/.ssh/pelikan_deploy root@vps-ip

Проверить Docker на VPS
systemctl status docker
docker ps

text

### Проблема: База данных пуста после перезапуска

Проверить volume
docker inspect pelikan-admin | grep -A 10 Mounts

Данные должны быть в
ls -la /opt/pelikan-admin-docker/data/

Если данные потеряны, восстановить из бэкапа
text

---

## 📦 Бэкапы

### Ручной бэкап

На VPS
cd /opt/pelikan-admin-docker

Создать бэкап базы данных
cp data/database.sqlite data/database.sqlite.backup-$(date +%Y%m%d-%H%M%S)

Создать полный бэкап проекта
tar -czf /backup/pelikan-$(date +%Y%m%d).tar.gz
--exclude='.git'
--exclude='node_modules'
/opt/pelikan-admin-docker/

text

### Автоматический бэкап (crontab)

Добавить в crontab
crontab -e

Бэкап каждый день в 2:00 ночи
0 2 * * * cd /opt/pelikan-admin-docker && cp data/database.sqlite data/backup-$(date +%Y%m%d).sqlite && find data/ -name "backup-*.sqlite" -mtime +7 -delete

text

### Восстановление из бэкапа

Остановить приложение
docker-compose down

Восстановить базу данных
cp data/database.sqlite.backup-20251110 data/database.sqlite

Запустить приложение
docker-compose up -d

text

---

## 🔐 Безопасность

### Рекомендации

- ✅ Используйте Let's Encrypt для production
- ✅ Регулярно обновляйте зависимости: `npm update`
- ✅ Ротируйте SSH ключи каждые 90 дней
- ✅ Используйте сильные пароли для Docker Hub
- ✅ Ограничьте доступ к VPS с помощью firewall
- ✅ Настройте регулярные бэкапы
- ✅ Мониторьте логи на подозрительную активность

### Обновление до Let's Encrypt

На VPS
apt install certbot

Получить сертификат
certbot certonly --standalone -d yourdomain.com

Обновить docker-compose.yml
volumes:
- /etc/letsencrypt/live/yourdomain.com/privkey.pem:/app/privkey.pem:ro
- /etc/letsencrypt/live/yourdomain.com/fullchain.pem:/app/fullchain.pem:ro
Настроить автообновление
echo "0 0 1 * * certbot renew --quiet" | crontab -

text

---

## 📝 Лицензия

MIT License

---

## 👤 Автор

**Andrey Hiitola**
- GitHub: [@Andreyhiitola](https://github.com/Andreyhiitola)
- Repository: [pelikan-admin-docker](https://github.com/Andreyhiitola/pelikan-admin-docker)

---

## 🎓 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Node.js Documentation](https://nodejs.org/docs/)
- [Express.js Guide](https://expressjs.com/en/guide/routing.html)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)

---

**Дата последнего обновления:** 10 ноября 2025
Сохраните этот файл как PROJECT.md:

bash
# Создайте файл
nano PROJECT.md

# Вставьте содержимое выше
# Сохраните: Ctrl+O, Enter, Ctrl+X

# Закоммитьте
git add PROJECT.md
git commit -m "Add comprehensive PROJECT.md documentation"
git push origin main
Теперь у вас полная документация проекта! 📚✨


Отлично! Создам обновленный **PROJECT.md** с полным описанием интеграции с GitHub API:

```markdown
# Pelikan Admin Docker - Полная техническая документация

Система управления контентом с автоматической синхронизацией в GitHub для сайта и мобильного приложения.

## 📋 Содержание

- [О проекте](#о-проекте)
- [Архитектура системы](#архитектура-системы)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [API Endpoints](#api-endpoints)
- [GitHub интеграция](#github-интеграция)
- [CI/CD Pipeline](#cicd-pipeline)
- [Деплой](#деплой)
- [Использование](#использование)
- [Мониторинг](#мониторинг)
- [Troubleshooting](#troubleshooting)
- [Безопасность](#безопасность)

---

## 🎯 О проекте

**Pelikan Admin Docker** — это полнофункциональная система управления контентом (CMS) для экосистемы "Пеликан Алаколь", включающей:

- 🌐 **Сайт:** [pelikan-alakol-site](https://github.com/Andreyhiitola/pelikan-alakol-site)
- 📱 **Android приложение:** [pelikan-android-app](https://github.com/Andreyhiitola/pelikan-android-app)

### Основные возможности

- ✏️ Редактирование контента через веб-интерфейс
- 💾 Локальное хранение в SQLite
- 🔄 Автоматическая синхронизация с GitHub
- 🔐 HTTPS из коробки
- 🐳 Полная контейнеризация
- 🚀 CI/CD автоматический деплой
- 📊 История изменений

---

## 🏗 Архитектура системы

### Общая схема

```
┌─────────────────────────────────────────────┐
│         Администратор                       │
│         (Web Browser)                       │
└────────────────┬────────────────────────────┘
                 │ HTTPS :8443
                 ▼
┌─────────────────────────────────────────────┐
│   pelikan-admin-docker (VPS)                │
│   ┌───────────────────────────────────┐     │
│   │  Node.js + Express                │     │
│   │  - REST API                       │     │
│   │  - Admin UI                       │     │
│   │  - SQLite Database                │     │
│   └───────────┬───────────────────────┘     │
│               │                             │
│               │ @octokit/rest               │
└───────────────┼─────────────────────────────┘
                │
                │ GitHub API
                │
         ┌──────┴──────┐
         │             │
         ▼             ▼
┌─────────────┐  ┌─────────────┐
│  alakol-    │  │  android-   │
│  site       │  │  app        │
│  (GitHub)   │  │  (GitHub)   │
│             │  │             │
│ data/       │  │ shared-     │
│  menu.json  │  │  content/   │
│  schedule...│  │  data/      │
└─────────────┘  └─────────────┘
       │                │
       ▼                ▼
   Веб-сайт      Android APK
```

### Поток данных

1. **Редактирование:** Администратор вносит изменения в админ-панели
2. **Сохранение:** Данные сохраняются в SQLite БД на VPS
3. **Синхронизация:** Нажатие кнопки "Sync to GitHub"
4. **GitHub API:** Обновление JSON файлов в репозиториях
5. **Публикация:** Сайт и приложение автоматически получают обновления

---

## 🛠 Технологический стек

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| Node.js | 18+ | Runtime environment |
| Express.js | 4.18+ | Web framework |
| SQLite3 | 5.1+ | База данных |
| @octokit/rest | 20.0+ | GitHub API клиент |
| https | built-in | SSL/TLS сервер |

### Frontend

| Технология | Назначение |
|------------|------------|
| HTML5 | Структура UI |
| CSS3 | Стилизация |
| Vanilla JavaScript | Логика клиента |
| Fetch API | HTTP запросы |

### DevOps

| Технология | Назначение |
|------------|------------|
| Docker | Контейнеризация |
| Docker Compose | Оркестрация |
| GitHub Actions | CI/CD |
| SSH | Удаленный деплой |
| OpenSSL | SSL сертификаты |

---

## 📁 Структура проекта

```
pelikan-admin-docker/
├── .github/
│   └── workflows/
│       └── ci-cd.yml              # GitHub Actions workflow
│
├── public/                         # Frontend
│   ├── index.html                 # Admin UI
│   ├── app.js                     # Client logic
│   └── styles.css                 # (опционально)
│
├── data/                           # Данные (не коммитится)
│   ├── database.sqlite            # SQLite БД
│   └── backups/                   # Резервные копии
│
├── server.js                       # Node.js сервер
├── package.json                    # NPM зависимости
├── package-lock.json               # Lockfile
│
├── Dockerfile                      # Docker образ
├── docker-compose.yml              # Compose конфигурация
│
├── privkey.pem                     # SSL ключ (не коммитится)
├── fullchain.pem                   # SSL сертификат (не коммитится)
│
├── .env                            # Переменные окружения (не коммитится)
├── .env.example                    # Пример .env
├── .gitignore                      # Игнорируемые файлы
│
├── README.md                       # Краткая документация
├── PROJECT.md                      # Эта документация
└── setup.sh                        # Скрипт установки
```

---

## 🚀 Установка

### Предварительные требования

- **VPS/Server:** Ubuntu 20.04+ или Debian 11+
- **Docker:** 20.10+
- **Docker Compose:** 2.0+
- **Git:** 2.0+
- **GitHub Personal Access Token** с правами `repo`

### Шаг 1: Подготовка VPS

```
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить Docker
curl -fsSL https://get.docker.com | sh
sudo systemctl start docker
sudo systemctl enable docker

# Установить Docker Compose
sudo apt install docker-compose -y

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверить установку
docker --version
docker-compose --version
```

### Шаг 2: Клонирование репозитория

```
# Создать директорию проекта
sudo mkdir -p /opt/pelikan-admin-docker
sudo chown $USER:$USER /opt/pelikan-admin-docker
cd /opt/pelikan-admin-docker

# Клонировать репозиторий
git clone https://github.com/Andreyhiitola/pelikan-admin-docker.git .

# Создать директорию для данных
mkdir -p data
```

### Шаг 3: SSL сертификаты

#### Вариант A: Self-signed (для тестирования)

```
openssl req -x509 -newkey rsa:2048 -nodes \
  -keyout privkey.pem \
  -out fullchain.pem \
  -days 365 \
  -subj "/CN=localhost"

chmod 600 privkey.pem
chmod 644 fullchain.pem
```

#### Вариант B: Let's Encrypt (для production)

```
# Установить Certbot
sudo apt install certbot -y

# Получить сертификат
sudo certbot certonly --standalone -d yourdomain.com

# Скопировать сертификаты
sudo cp /etc/letsencrypt/live/yourdomain.com/privkey.pem ./
sudo cp /etc/letsencrypt/live/yourdomain.com/fullchain.pem ./
sudo chown $USER:$USER *.pem
```

### Шаг 4: Переменные окружения

```
# Создать .env файл
cp .env.example .env
nano .env
```

Заполните:

```
NODE_ENV=production
PORT=8443

# GitHub Integration
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_OWNER=Andreyhiitola
REPO_SITE=pelikan-alakol-site
REPO_APP=pelikan-android-app
```

### Шаг 5: Установка зависимостей

```
# Локально (для разработки)
npm install

# Или через Docker (для production)
docker-compose build
```

### Шаг 6: Firewall

```
# Открыть порт 8443
sudo ufw allow 8443/tcp
sudo ufw reload

# Проверить статус
sudo ufw status
```

### Шаг 7: Запуск

```
# Запустить контейнеры
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f
```

### Шаг 8: Проверка

```
# Проверить порт
ss -tulpn | grep 8443

# Тест подключения
curl -k https://localhost:8443

# Открыть в браузере
https://YOUR_VPS_IP:8443
```

---

## ⚙️ Конфигурация

### package.json

```
{
  "name": "pelikan-admin-docker",
  "version": "1.0.0",
  "description": "Admin panel with GitHub sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "@octokit/rest": "^20.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
```

### docker-compose.yml

```
version: '3.8'

services:
  app:
    image: andreysagurov/pelikan-admin:latest
    container_name: pelikan-admin
    restart: unless-stopped
    ports:
      - "8443:8443"
    volumes:
      - ./data:/app/data
      - ./privkey.pem:/app/privkey.pem:ro
      - ./fullchain.pem:/app/fullchain.pem:ro
    environment:
      - NODE_ENV=production
      - PORT=8443
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_OWNER=${GITHUB_OWNER}
      - REPO_SITE=${REPO_SITE}
      - REPO_APP=${REPO_APP}
    env_file:
      - .env
    networks:
      - pelikan-network

networks:
  pelikan-network:
    driver: bridge
```

### Dockerfile

```
FROM node:18-alpine

WORKDIR /app

# Копировать package files
COPY package*.json ./

# Установить зависимости
RUN npm ci --only=production

# Копировать код приложения
COPY server.js ./
COPY public ./public

# Создать директорию для данных
RUN mkdir -p /app/data

EXPOSE 8443

CMD ["node", "server.js"]
```

---

## 🌐 API Endpoints

### Базовый URL

```
https://YOUR_VPS_IP:8443/api
```

### Endpoints для данных

#### GET /api/:section

Получить данные раздела из БД

**Пример:**
```
curl -k https://localhost:8443/api/menu
```

**Response:**
```
{
  "items": [
    {
      "name": "Борщ",
      "price": 350,
      "description": "Украинский борщ"
    }
  ]
}
```

#### POST /api/:section

Сохранить данные раздела в БД

**Пример:**
```
curl -k -X POST https://localhost:8443/api/menu \
  -H "Content-Type: application/json" \
  -d '{"items":[{"name":"Борщ","price":350}]}'
```

**Response:**
```
{
  "success": true,
  "message": "Data saved to database"
}
```

### Endpoints для синхронизации

#### POST /api/sync/:section

Синхронизировать раздел с GitHub

**Пример:**
```
curl -k -X POST https://localhost:8443/api/sync/menu
```

**Response:**
```
{
  "success": true,
  "message": "Synced to GitHub successfully",
  "results": [
    {
      "repo": "pelikan-alakol-site",
      "path": "data/menu.json",
      "success": true,
      "url": "https://github.com/..."
    },
    {
      "repo": "pelikan-android-app",
      "path": "shared-content/data/menu.json",
      "success": true,
      "url": "https://github.com/..."
    }
  ]
}
```

#### POST /api/sync-all

Синхронизировать все разделы с GitHub

**Response:**
```
{
  "success": true,
  "results": [
    {"section": "menu", "success": true},
    {"section": "schedule", "success": true},
    {"section": "accommodation", "success": true}
  ]
}
```

---

## 🔗 GitHub интеграция

### Создание GitHub Token

1. Откройте: https://github.com/settings/tokens
2. **Generate new token (classic)**
3. **Note:** `Pelikan Admin Panel`
4. **Expiration:** `No expiration` или `90 days`
5. **Scopes:** Выберите `repo` (полный доступ)
6. **Generate token**
7. **Скопируйте токен:** `ghp_xxxx...`

### Настройка токена

```
# На VPS
cd /opt/pelikan-admin-docker
nano .env
```

Добавьте:
```
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### Проверка токена

```
# Тест через curl
curl -H "Authorization: token YOUR_TOKEN" \
  https://api.github.com/user

# Должно вернуть информацию о вашем аккаунте
```

### Структура синхронизации

#### Для pelikan-alakol-site

```
pelikan-alakol-site/
└── data/
    ├── menu.json
    ├── schedule.json
    ├── accommodation.json
    ├── infrastructure.json
    ├── activities.json
    └── contacts.json
```

#### Для pelikan-android-app

```
pelikan-android-app/
└── shared-content/
    └── data/
        ├── menu.json
        ├── schedule.json
        └── ...
```

### Пример JSON структуры

**menu.json:**
```
{
  "categories": [
    {
      "name": "Горячие блюда",
      "items": [
        {
          "name": "Борщ украинский",
          "price": 350,
          "description": "С пампушками",
          "available": true
        }
      ]
    }
  ],
  "updated_at": "2025-11-10T16:30:00Z"
}
```

---

## 🔄 CI/CD Pipeline

### GitHub Actions Workflow

Файл: `.github/workflows/ci-cd.yml`

```
name: Build and Deploy to VPS

on:
  push:
    branches: [main]

env:
  IMAGE_NAME: pelikan-admin

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: andreysagurov/pelikan-admin:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 300s
          command_timeout: 120s
          script: |
            cd /opt/pelikan-admin-docker
            git pull origin main
            docker pull andreysagurov/pelikan-admin:latest
            docker-compose down --remove-orphans
            docker-compose up -d --remove-orphans
            docker-compose ps
```

### GitHub Secrets

Необходимо добавить в **Settings → Secrets → Actions**:

```
DOCKER_PASSWORD       # Docker Hub пароль
VPS_HOST             # IP адрес VPS
VPS_USER             # SSH пользователь (root)
VPS_SSH_KEY          # Приватный SSH ключ (весь текст)
```

### GitHub Variables

**Settings → Secrets → Actions → Variables**:

```
DOCKERHUB_USERNAME   # Логин Docker Hub
```

---

## 🚢 Деплой

### Автоматический деплой

```
# Внести изменения в код
git add .
git commit -m "Update feature"
git push origin main

# Workflow автоматически:
# 1. Соберет Docker образ
# 2. Загрузит в Docker Hub
# 3. Задеплоит на VPS
# 4. Перезапустит контейнеры
```

### Ручной деплой

```
# Подключиться к VPS
ssh root@vps-ip

cd /opt/pelikan-admin-docker

# Обновить код
git pull origin main

# Скачать новый образ
docker pull andreysagurov/pelikan-admin:latest

# Перезапустить
docker-compose down
docker-compose up -d

# Проверить
docker-compose ps
docker-compose logs --tail=50
```

### Откат к предыдущей версии

```
# На VPS
cd /opt/pelikan-admin-docker

# Посмотреть историю
git log --oneline

# Откатиться к коммиту
git reset --hard <commit-hash>

# Перезапустить
docker-compose up -d --force-recreate
```

---

## 📖 Использование

### Workflow администратора

1. **Открыть админ-панель:**
   ```
   https://YOUR_VPS_IP:8443
   ```

2. **Редактировать раздел:**
   - Выбрать раздел (меню, расписание, и т.д.)
   - Внести изменения в textarea
   - Нажать **"Сохранить"**

3. **Синхронизировать с GitHub:**
   - Нажать **"Sync to GitHub"** на конкретном разделе
   - ИЛИ **"Синхронизировать все"** внизу страницы

4. **Проверить результат:**
   - Открыть GitHub репозиторий
   - Убедиться, что файлы обновились
   - Проверить сайт/приложение

### Примеры редактирования

#### Меню ресторана

```
{
  "categories": [
    {
      "name": "Завтраки",
      "items": [
        {
          "name": "Омлет с грибами",
          "price": 250,
          "description": "3 яйца, шампиньоны, зелень"
        }
      ]
    }
  ]
}
```

#### Расписание мероприятий

```
{
  "activities": [
    {
      "time": "10:00",
      "title": "Аквааэробика",
      "location": "Бассейн",
      "duration": "60 минут"
    }
  ]
}
```

---

## 📊 Мониторинг

### Проверка статуса

```
# Статус контейнеров
docker-compose ps

# Логи в реальном времени
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Использование ресурсов
docker stats pelikan-admin
```

### Проверка работоспособности

```
# Тест API
curl -k https://localhost:8443/api/menu

# Проверка порта
ss -tulpn | grep 8443

# Проверка SSL
openssl s_client -connect localhost:8443
```

### Мониторинг синхронизации

```
# Просмотр логов синхронизации
docker-compose logs | grep "Sync"

# Проверка последних коммитов в GitHub
curl -H "Authorization: token YOUR_TOKEN" \
  https://api.github.com/repos/Andreyhiitola/pelikan-alakol-site/commits
```

---

## 🔧 Troubleshooting

### Контейнер не запускается

```
# Проверить логи
docker-compose logs

# Проверить образ
docker images | grep pelikan-admin

# Пересоздать контейнер
docker-compose down
docker-compose up -d --force-recreate
```

### Порт не отвечает

```
# Проверить, что контейнер работает
docker-compose ps  # STATUS = Up

# Проверить firewall
sudo ufw status
sudo ufw allow 8443/tcp

# Проверить, не занят ли порт
sudo lsof -i :8443
```

### Ошибка синхронизации с GitHub

```
# Проверить токен
docker-compose exec app printenv GITHUB_TOKEN

# Проверить права токена
curl -H "Authorization: token YOUR_TOKEN" \
  https://api.github.com/user

# Проверить логи
docker-compose logs | grep "GitHub"
```

### База данных повреждена

```
# Остановить контейнер
docker-compose down

# Восстановить из бэкапа
cp data/backups/database.sqlite.backup data/database.sqlite

# Запустить
docker-compose up -d
```

### SSL сертификат истек

```
# Обновить Let's Encrypt
sudo certbot renew

# Скопировать новые сертификаты
sudo cp /etc/letsencrypt/live/yourdomain.com/*.pem ./

# Перезапустить
docker-compose restart
```

---

## 🔐 Безопасность

### Рекомендации

- ✅ **Используйте Let's Encrypt** для production
- ✅ **Ограничьте доступ** по IP через firewall
- ✅ **Регулярно обновляйте** Docker и зависимости
- ✅ **Ротируйте GitHub Token** каждые 90 дней
- ✅ **Создавайте бэкапы** ежедневно
- ✅ **Мониторьте логи** на подозрительную активность
- ✅ **Используйте сильные пароли** для VPS

### Защита .env файла

```
# Ограничить доступ
chmod 600 .env

# Проверить, что не закоммичен
cat .gitignore | grep .env

# Удалить из истории Git (если случайно закоммитили)
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch .env' \
  --prune-empty --tag-name-filter cat -- --all
```

### Firewall настройка

```
# Разрешить только необходимые порты
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 8443/tcp
sudo ufw enable

# Ограничить доступ по IP (опционально)
sudo ufw allow from YOUR_IP to any port 8443
```

### Fail2Ban (защита от брутфорса)

```
# Установить Fail2Ban
sudo apt install fail2ban -y

# Настроить для SSH
sudo nano /etc/fail2ban/jail.local
```

Добавить:
```
[sshd]
enabled = true
port = ssh
filter = sshd
logpath = /var/log/auth.log
maxretry = 3
bantime = 3600
```

---

## 📦 Бэкапы

### Ручной бэкап

```
# Бэкап базы данных
cd /opt/pelikan-admin-docker
cp data/database.sqlite data/backups/database-$(date +%Y%m%d-%H%M).sqlite

# Бэкап всего проекта
tar -czf /backup/pelikan-$(date +%Y%m%d).tar.gz \
  --exclude='.git' \
  --exclude='node_modules' \
  /opt/pelikan-admin-docker/
```

### Автоматический бэкап (cron)

```
# Создать скрипт бэкапа
nano /opt/backup-pelikan.sh
```

Содержимое:
```
#!/bin/bash
DATE=$(date +%Y%m%d-%H%M)
cd /opt/pelikan-admin-docker
cp data/database.sqlite data/backups/database-$DATE.sqlite
find data/backups/ -name "database-*.sqlite" -mtime +7 -delete
```

Добавить в crontab:
```
chmod +x /opt/backup-pelikan.sh
crontab -e

# Добавить строку (бэкап каждый день в 2:00)
0 2 * * * /opt/backup-pelikan.sh
```

### Восстановление

```
# Остановить приложение
docker-compose down

# Восстановить БД
cp data/backups/database-20251110-1400.sqlite data/database.sqlite

# Запустить
docker-compose up -d
```

---

## 🎓 Дополнительные материалы

### Связанные репозитории

- **Сайт:** https://github.com/Andreyhiitola/pelikan-alakol-site
- **Android приложение:** https://github.com/Andreyhiitola/pelikan-android-app
- **Первая админка (статическая):** https://github.com/Andreyhiitola/pelikan-admin

### Документация технологий

- [Node.js Documentation](https://nodejs.org/docs/)
- [Express.js Guide](https://expressjs.com/)
- [Octokit GitHub API](https://octokit.github.io/rest.js/)
- [Docker Documentation](https://docs.docker.com/)
- [GitHub Actions](https://docs.github.com/en/actions)

### Полезные ссылки

- [GitHub API Reference](https://docs.github.com/en/rest)
- [SQLite Documentation](https://www.sqlite.org/docs.html)
- [Let's Encrypt](https://letsencrypt.org/docs/)

---

## 📝 Changelog

### v1.0.0 (2025-11-10)

- ✅ Первый релиз
- ✅ CI/CD через GitHub Actions
- ✅ Docker контейнеризация
- ✅ GitHub API интеграция
- ✅ SSL поддержка
- ✅ Автоматическая синхронизация с репозиториями

---

## 👥 Участие в проекте

Приветствуются pull requests и предложения по улучшению!

### Roadmap

- [ ] JWT аутентификация
- [ ] Загрузка изображений
- [ ] WYSIWYG редактор
- [ ] История изменений с diff
- [ ] Webhook для автоматического pull
- [ ] Поддержка нескольких пользователей
- [ ] Мультиязычность интерфейса

---

## 📄 Лицензия

MIT License

---

## 📧 Контакты

**Автор:** Andrey Hiitola  
**GitHub:** [@Andreyhiitola](https://github.com/Andreyhiitola)  
**Repository:** [pelikan-admin-docker](https://github.com/Andreyhiitola/pelikan-admin-docker)

---

**Дата последнего обновления:** 10 ноября 2025, 19:35 MSK
```

Сохраните этот PROJECT.md:

```bash
nano PROJECT.md
# Вставьте содержимое

git add PROJECT.md
git commit -m "Add comprehensive PROJECT.md with GitHub integration"
git push origin main
```

Теперь у вас полная документация с интеграцией GitHub API! 📚✨








